info:
  name: Create Tenant
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/management/tenants"
  body:
    type: json
    data: |-
      {
          "name": "test-tenant"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
