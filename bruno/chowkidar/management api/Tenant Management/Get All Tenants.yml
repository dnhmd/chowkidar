info:
  name: Get All Tenants
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/management/tenants "
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
